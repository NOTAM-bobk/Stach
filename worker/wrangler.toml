name = "staches-api"
main = "index.js"
compatibility_date = "2024-11-01"

[[d1_databases]]
binding = "DB"
database_name = "staches-db"
database_id = "REPLACE_WITH_YOUR_D1_DATABASE_ID"

# Set with: wrangler secret put JWT_SECRET
# or add it in the Cloudflare dashboard under Worker > Settings > Variables (as a secret)
